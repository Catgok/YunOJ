syntax = "v1"

info(
	title: "test api"
	desc: "test's api desc'"
	author: "catgok"
	email: "catgok@163.com"
	version: "0.1"
)

type request {
	Id int64 `json:"id"`
}

type response {
	Info string `json:"info"`
}
//
//@server(
//    jwt: Auth
//    group: template
//)
service test {
	@handler getOne
	post /query (request) returns (response)
}